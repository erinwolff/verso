# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node / Vite
node_modules/
frontend/dist/
backend/static/
*.tsbuildinfo

# Data volume (never commit journal entries)
data/
*.local

# Local compose holds the real VERSO_PASSWORD — keep it out of git.
# compose.example.yaml is the committed template.
compose.yaml

# Editor / OS
.DS_Store
.vscode/
.idea/
